import React from 'react';

import Store from 'store/context';

const Home = () => {
  const { data } = React.useContext(Store);
  const appName = data.appName;

  return <div id="home-page">Hello {appName}</div>;
};

export default Home;
